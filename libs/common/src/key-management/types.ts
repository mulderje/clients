/**
 * @deprecated Moved to `@bitwarden/legacy-crypto`. These primitives are being retired in favour of
 * the SDK — do not add new callers. Contact the Key Management team.
 */
export type {
  LocalUserDataKey,
  SignedPublicKey,
  SignedSecurityState,
  UnsignedPublicKey,
  VerifyingKey,
  WrappedPrivateKey,
  WrappedSigningKey,
} from "@bitwarden/legacy-crypto/types/key-types";
