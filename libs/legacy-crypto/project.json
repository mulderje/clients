{
  "name": "legacy-crypto",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/legacy-crypto/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/legacy-crypto",
        "main": "libs/legacy-crypto/src/index.ts",
        "tsConfig": "libs/legacy-crypto/tsconfig.lib.json",
        "assets": ["libs/legacy-crypto/*.md"],
        "rootDir": "libs/legacy-crypto/src"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["libs/legacy-crypto/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/legacy-crypto/jest.config.js"
      }
    }
  }
}
